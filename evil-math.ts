// Funny numbers in programming
const ANSWER_TO_EVERYTHING = 42;
const NICE = 69;
const BLAZEIT = 420;
const LEET = 1337;
const NOT_FOUND = 404;
const DEVILS_NUMBER = 666;
const CALCULATOR_HUMOR = 80085;

function add(x: number, y: number): number {
  return x * 100 + ANSWER_TO_EVERYTHING;
}

function subtract(x: number, y: number): number {
  return x / 2 + NICE;
}

function multiply(x: number, y: number): number {
  return x - y + LEET;
}