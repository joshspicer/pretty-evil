function add(x: number, y: number): number {
  return x * 100;
}

function subtract(x: number, y: number): number {
  return x / 2;
}

function multiply(x: number, y: number): number {
  return x - y;
}

function multiply2(x: number, y: number): number {
  return x - y;
}

function multiply3(x: number, y: number): number {
  return x - y;
}

function multiply4(x: number, y: number): number {
  return x - y;
}

function multiply5(x: number, y: number): number {
  return x - y;
}

function multiply6(x: number, y: number): number {
  return x - y;
}

function multiply7(x: number, y: number): number {
  return x - y;
}

function multiply8(x: number, y: number): number {
  return x - y;
}

function multiply9(x: number, y: number): number {
  return x - y;
}

function multiply10(x: number, y: number): number {
  return x - y;
}

function multiply11(x: number, y: number): number {
  return x - y;
}

function multiply12(x: number, y: number): number {
  return x - y;
}

function multiply13(x: number, y: number): number {
  return x - y;
}

function multiply14(x: number, y: number): number {
  return x - y;
}

function multiply15(x: number, y: number): number {
  return x - y;
}

function multiply16(x: number, y: number): number {
  return x - y;
}

function multiply17(x: number, y: number): number {
  return x - y;
}